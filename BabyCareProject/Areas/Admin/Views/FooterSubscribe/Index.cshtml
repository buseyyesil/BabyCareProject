@* @model List<ResultFooterSubscribeDto>

@{
    ViewData["Title"] = "Bülten Aboneleri";
    Layout = "~/Areas/Admin/Views/AdminLayout/Layout.cshtml";
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title d-flex justify-content-between align-items-center">
            <span><i class="mdi mdi-email-newsletter"></i> Bülten Aboneleri</span>
            <span class="badge badge-primary">Toplam: @Model.Count</span>
        </h4>

        @if (Model != null && Model.Count > 0)
        {
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>E-posta</th>
                            <th>Kayıt Tarihi</th>
                            <th width="100">Durum</th>
                            <th width="150">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td><strong>@item.Email</strong></td>
                                <td>@item.SubscribeDate.ToString("dd/MM/yyyy HH:mm")</td>
                                <td>
                                    @if (item.IsActive)
                                    {
                                        <span class="badge badge-success">Aktif</span>
                                    }
                                    else
                                    {
                                        <span class="badge badge-danger">Pasif</span>
                                    }
                                </td>
                                <td>
                                    <a href="/Admin/FooterSubscribe/UpdateFooterSubscribe/@item.FooterSubscribeId" class="btn btn-warning btn-sm">
                                        <i class="mdi mdi-pencil"></i>
                                    </a>
                                    <a href="/Admin/FooterSubscribe/DeleteFooterSubscribe/@item.FooterSubscribeId"
                                       class="btn btn-danger btn-sm"
                                       onclick="return confirm('Bu aboneyi silmek istediğinize emin misiniz?')">
                                        <i class="mdi mdi-delete"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="alert alert-info">
                <i class="mdi mdi-information"></i> Henüz abone kaydı yok.
            </div>
        }
    </div>
</div> *@